# KBEX.io - Docker Compose para Produção
version: '3.8'

services:
  # MongoDB Database
  mongodb:
    image: mongo:6.0
    container_name: kbex-mongodb
    restart: always
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=kbex_production
    networks:
      - kbex-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # FastAPI Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: kbex-backend
    restart: always
    ports:
      - "8001:8001"
    depends_on:
      mongodb:
        condition: service_healthy
    environment:
      - MONGO_URL=mongodb://mongodb:27017
      - DB_NAME=kbex_production
      - CORS_ORIGINS=${DOMAIN_URL:-*}
      - JWT_SECRET=${JWT_SECRET}
      - COINMARKETCAP_API_KEY=${COINMARKETCAP_API_KEY}
      - BINANCE_API_KEY=${BINANCE_API_KEY}
      - BINANCE_SECRET_KEY=${BINANCE_SECRET_KEY}
      - FIREBLOCKS_API_KEY=${FIREBLOCKS_API_KEY:-}
      - FIREBLOCKS_SECRET_KEY=${FIREBLOCKS_SECRET_KEY:-}
      - FIREBLOCKS_SECRET_KEY_PATH=${FIREBLOCKS_SECRET_KEY_PATH:-/app/secrets/fireblocks.key}
      - FIREBLOCKS_BASE_PATH=${FIREBLOCKS_BASE_PATH:-https://api.fireblocks.io}
      - SUMSUB_APP_TOKEN=${SUMSUB_APP_TOKEN}
      - SUMSUB_SECRET_KEY=${SUMSUB_SECRET_KEY}
      - SUMSUB_LEVEL_NAME=${SUMSUB_LEVEL_NAME:-basic-kyc-level}
      - BREVO_API_KEY=${BREVO_API_KEY}
      - BREVO_SENDER_EMAIL=${BREVO_SENDER_EMAIL:-no-reply@kbex.io}
      - BREVO_SENDER_NAME=${BREVO_SENDER_NAME:-KBEX}
      - STRIPE_API_KEY=${STRIPE_API_KEY:-}
      - FRONTEND_URL=${FRONTEND_URL:-https://kbex.io}
      - TURNSTILE_SECRET_KEY=${TURNSTILE_SECRET_KEY:-}
      - AZURE_TENANT_ID=${AZURE_TENANT_ID:-}
      - AZURE_CLIENT_ID=${AZURE_CLIENT_ID:-}
      - AZURE_CLIENT_SECRET=${AZURE_CLIENT_SECRET:-}
      - AZURE_REDIRECT_URI=${AZURE_REDIRECT_URI:-}
      - TRUSTFULL_API_KEY=${TRUSTFULL_API_KEY:-}
      - REVOLUT_CLIENT_ID=${REVOLUT_CLIENT_ID}
      - REVOLUT_REDIRECT_URI=${REVOLUT_REDIRECT_URI:-https://kbex.io/api/revolut/callback}
      - REVOLUT_ENV=${REVOLUT_ENV:-production}
    volumes:
      - backend_uploads:/app/uploads
      - ./backend/secrets:/app/secrets:ro
      - ./backend/certs:/app/certs:ro
    networks:
      - kbex-network
    healthcheck:
      # The python:3.11-slim image does NOT include `curl`, so the previous
      # probe always failed → container reported "unhealthy" despite serving
      # 200 OK. Use the stdlib `urllib` to avoid installing extra packages.
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8001/api/health',timeout=5).status==200 else 1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # React Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_BACKEND_URL=${DOMAIN_URL:-http://localhost}
    container_name: kbex-frontend
    restart: always
    depends_on:
      - backend
    networks:
      - kbex-network

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: kbex-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certbot/conf:/etc/nginx/ssl:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./maintenance.html:/usr/share/nginx/html/maintenance.html:ro
    depends_on:
      - backend
      - frontend
    networks:
      - kbex-network

  # Certbot for SSL (Let's Encrypt)
  certbot:
    image: certbot/certbot
    container_name: kbex-certbot
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

volumes:
  mongodb_data:
  backend_uploads:

networks:
  kbex-network:
    driver: bridge
